module; // important to be before includes

export module test;

// Implementação das funções declaradas na interface do módulo 'test'
export int add(int a, int b) {
    return a + b;
}

export int multiply(int a, int b) {
    return a * b;
}
